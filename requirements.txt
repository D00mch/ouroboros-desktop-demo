openai>=1.66.0
requests
httpx
dulwich
starlette
python-multipart
uvicorn[standard]
websockets
huggingface_hub
PyYAML>=6.0
claude-agent-sdk>=0.1.60
dialog-bot-sdk>=3.0.8
grpcio
grpcio-tools
protobuf
googleapis-common-protos
openpyxl>=3.1.0
# optional: A2A (Agent-to-Agent) protocol — pip install 'ouroboros[a2a]'
# a2a-sdk[http-server]>=0.3.20
# optional: browser automation tools (for browse_page/browser_action)
# pip install '.[browser]' && python3 -m playwright install chromium
# optional: local model server (requires Metal-enabled build on macOS)
# CMAKE_ARGS="-DGGML_METAL=on" pip install llama-cpp-python[server]
